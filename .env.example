# This file is a placeholder for environment variables required to run this project.

# Password for protected pages/routes (legacy RouteGuard)
PAGE_ACCESS_PASSWORD=password

# NextAuth.js Configuration
# Generate a secret with: openssl rand -base64 32
AUTH_SECRET=your_auth_secret_here

# Google OAuth Provider
# Create credentials at: https://console.cloud.google.com/apis/credentials
AUTH_GOOGLE_ID=your_google_client_id
AUTH_GOOGLE_SECRET=your_google_client_secret

# Admin Access Control
# Comma-separated list of email addresses allowed to access /admin
ADMIN_EMAILS=admin@example.com,another@example.com

# URL of this project
NEXTAUTH_URL=http://localhost:3000

# Public site URL used by metadata, robots, and sitemap
# Keep SITE_INDEXABLE=false until the portfolio is ready to be crawled.
SITE_URL=http://localhost:3000
SITE_INDEXABLE=false

# Database (PostgreSQL via Docker)
# Start PostgreSQL with: docker-compose up -d
DATABASE_URL="postgresql://portfolio_user:portfolio_password@localhost:5432/magic_portfolio?schema=public"

# S3-Compatible Storage Configuration
# Local Docker Compose uses Garage at http://127.0.0.1:3900.
# Also works with AWS S3, Cloudflare R2, DigitalOcean Spaces, Backblaze B2, etc.
S3_ENDPOINT=http://127.0.0.1:3900
S3_REGION=garage
S3_ACCESS_KEY_ID=GK0123456789abcdef01234567
S3_SECRET_ACCESS_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
S3_BUCKET_NAME=portfolio-images
# Optional: Custom public URL for CDN, custom domain, or the app image proxy
S3_PUBLIC_URL=/api/images
# Set to "true" for Garage, Cloudflare R2, or other path-style providers
S3_FORCE_PATH_STYLE=true
